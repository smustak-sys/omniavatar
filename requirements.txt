librosa==0.10.2.post1
tqdm
peft==0.15.1
transformers==4.52.3
scipy==1.14.0
numpy==1.26.4
xfuser==0.4.1
ftfy
einops
# Implicit deps — imported by the upstream code but were not pinned.
# Pinning them here so a fresh `pip install -r requirements.txt` Just Works.
pyyaml>=6.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.1
soundfile>=0.12.1
Pillow>=10.0.0
safetensors>=0.4.3
